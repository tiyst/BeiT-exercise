spring:
  application:
    name: beit-exercise
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
  jpa:
    properties:
      hibernate:
        jdbc:
          batch_size: 30
        order_updates: true
